ProcDef("A",nopar,seqC(proc("LEFT",nopar),proc("RIGHT",nopar)))
ProcDef("LEFT",nopar,Prefix(Sigma("a"),Skip))
ProcDef("RIGHT",nopar,Prefix(Sigma("b"),Skip))
ProcHasDeadlock("A")
State(omega)
State(Prefix(Sigma("a"),Skip))
State(Prefix(Sigma("b"),Skip))
State(proc("LEFT",nopar))
State(proc("RIGHT",nopar))
State(seqC(Prefix(Sigma("a"),Skip),proc("RIGHT",nopar)))
State(seqC(proc("LEFT",nopar),proc("RIGHT",nopar)))
State(seqC(Skip,proc("RIGHT",nopar)))
State(Skip)
trans(State(Prefix(Sigma("a"),Skip)),Sigma("a"),State(Skip))
trans(State(Prefix(Sigma("b"),Skip)),Sigma("b"),State(Skip))
trans(State(proc("LEFT",nopar)),tau,State(Prefix(Sigma("a"),Skip)))
trans(State(proc("RIGHT",nopar)),tau,State(Prefix(Sigma("b"),Skip)))
trans(State(seqC(Prefix(Sigma("a"),Skip),proc("RIGHT",nopar))),Sigma("a"),State(seqC(Skip,proc("RIGHT",nopar))))
trans(State(seqC(proc("LEFT",nopar),proc("RIGHT",nopar))),tau,State(seqC(Prefix(Sigma("a"),Skip),proc("RIGHT",nopar))))
trans(State(seqC(Skip,proc("RIGHT",nopar))),tau,State(proc("RIGHT",nopar)))
trans(State(Skip),tick,State(omega))

